# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode
build/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.moved-aside
*.xccheckout
*.xcscmblueprint
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.xcresult
# Xcode regenerates the workspace inside a .xcodeproj whenever the project is opened.
# It is per-machine state and `xcodebuild -project` never reads it.
*.xcodeproj/project.xcworkspace/
*.xcodeproj/xcshareddata/xcdebugger/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved
# …except the app's own resolved versions: this is a product, not a library, so the
# Sparkle version is pinned deliberately, and the release workflow reads it back to pick
# the matching sign_update.
!Recordito.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# Fastlane / provisioning
*.mobileprovision
*.cer
*.p12

# Secrets — OAuth client config must never be committed
Secrets.xcconfig
# The Sparkle update-signing key, as exported by Scripts/generate-sparkle-keys.sh
sparkle_private_key.txt
GoogleService-Info.plist
client_secret*.json

# Recordings and scratch output
*.recordito/
*.mov
*.mp4
*.caf
