{
  "name": "ketto-worker-tests",
  "private": true,
  "description": "Test harness and local dev runner for Ketto/Resources/CloudflareBackend/worker.js. Dev-only; nothing here ships in the app.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.131.0"
  }
}
